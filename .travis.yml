language: php

php:
  - 7.2

before_script:
  - composer install

script:
  - mkdir -p build/logs
  - vendor/phpunit/phpunit/phpunit --coverage-clover build/logs/clover.xml
  - tail build/logs/clover.xml

after_success:
  - travis_retry php vendor/bin/php-coveralls
